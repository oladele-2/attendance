/** Open check-ins older than this are void: no check-out; next punch creates a new check-in. */
export const OPEN_SHIFT_MAX_HOURS = 20;
export const OPEN_SHIFT_MAX_MINUTES = OPEN_SHIFT_MAX_HOURS * 60;
